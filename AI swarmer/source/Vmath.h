#pragma once
#include <SFML\Graphics.hpp>


float magnitude(sf::Vector2f vector);

float angleOfVect(sf::Vector2f vector);

sf::Vector2f normalise(sf::Vector2f vector);

//void operator*= (sf::v double rhs);